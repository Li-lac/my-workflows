name: Build Final Config

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install axios js-yaml
      - run: node scripts/run-rewrite.js
        env:
          SUB_URL: "https://proxyinfo.net/api/v1/client/subscribe?token=e4921e435c8e2651c128f3e974a9cca4"
      - run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add output/
          git diff --quiet && git diff --staged --quiet || git commit -m "Update config.yaml"
          git push
